{
  "name": "request-hostname",
  "version": "0.0.0",
  "description": "Get a proper hostname off of a request object",
  "main": "index.js",
  "scripts": {
    "test": "happiness && mocha",
    "prepublish": "npm t && git push && git push --tags"
  },
  "keywords": [
    "request",
    "hostname",
    "host header",
    "http host",
    "host"
  ],
  "author": "Wes Todd",
  "license": "ISC",
  "devDependencies": {
    "happiness": "^5.5.0",
    "mocha": "^2.3.3"
  }
}
